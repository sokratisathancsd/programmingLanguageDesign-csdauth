#sigrisi stin assembly IF
#exw to jump!!! to simvolizw me j

#conditional 
#else block
#j end //to stelnw sto end, einai san to goto
#then:
#then block
#end:

#sigrisi metaksi register beq 
#beq $t0, $t1, then //a=b
#ta ypoloipa einai stis simeiwseis!
# beqz $t0, then // sugrinw me to 0

#thelw na kanw
#if(conditional)
	#then block
#else
	#else block
	
#oi entoles tis assembly einai entoles me kwdikes 3 dieythunsewn

    

